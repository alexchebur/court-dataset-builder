# ⚖️ Сборщик датасета судебных актов

Приложение для сбора и подготовки датасета судебных решений арбитражных судов для обучения ML-моделей (LoRA, Fine-tuning).

## 🚀 Развёртывание на Streamlit Cloud

### Шаг 1: Создайте репозиторий на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/your-username/court-dataset-builder.git
git push -u origin main
